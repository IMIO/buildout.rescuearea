[buildout]
extends =
    base.cfg

auto-checkout = *

allow-picked-versions = true
show-picked-versions = true

[instance]
debug-mode = on
verbose-security = on
user = admin:admin
eggs +=
    ${debug-products:eggs}

[debug-products]
eggs =
